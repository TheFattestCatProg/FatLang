package main;

using std;
using std:io;
using other;

i32 main() {
    //unsafe {
        i32 i = 0;
        void* p = (void*)&i;
    //}
    var p = point<i32>(5);
    io:println(p.getX());

    return 0;
}