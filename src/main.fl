package main;

using std;
using std:io;
using other;

/*
   enum Shape {
            Rect(i32, i32),
            Circle(i32)
   }
*/

i32 main() {
    i32 i = 0;
    unsafe {
        i32* p = &i;
    }
    var p = point(5);

    io:println(p.getX());

    return 0;
}